import { VoiceAnnotation } from "./VoiceAnnotation";

/**
 * VoiceAnnotations
 * @targetNSAlias `tns`
 * @targetNamespace `http://www.symbility.net/webservices/`
 */
export interface VoiceAnnotations {
    /** VoiceAnnotation[] */
    VoiceAnnotation?: Array<VoiceAnnotation>;
}
