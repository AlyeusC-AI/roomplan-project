import { PhotoSpecification } from "./PhotoSpecification";

/**
 * photoSpecifications
 * @targetNSAlias `tns`
 * @targetNamespace `http://www.symbility.net/webservices/`
 */
export interface PhotoSpecifications {
    /** PhotoSpecification[] */
    PhotoSpecification?: Array<PhotoSpecification>;
}
