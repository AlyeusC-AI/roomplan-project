import { ClaimCoverage } from "./ClaimCoverage";

/**
 * Coverages
 * @targetNSAlias `tns`
 * @targetNamespace `http://www.symbility.net/webservices/`
 */
export interface Coverages {
    /** ClaimCoverage[] */
    ClaimCoverage?: Array<ClaimCoverage>;
}
