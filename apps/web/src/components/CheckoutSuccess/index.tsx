
const CheckoutSuccess = () => {
  // const { loading, error, data = [] } = useFetch('/api/', options, [])

  return (
    <div className="flex flex-grow items-center justify-center bg-gray-50">
      <div>
        <h1>Payment Succesful!</h1>
      </div>
    </div>
  )
}

export default CheckoutSuccess
