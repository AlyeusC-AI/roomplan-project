buildscript {
    ext {
        kotlinVersion = '1.7.20'
    }
    dependencies {
        classpath("org.jetbrains.kotlin:kotlin-gradle-plugin:$kotlinVersion")
    }
}

apply from: new File(["node", "--print", "require.resolve('@expo/patches/kotlin-version.gradle')"].execute(null, rootDir).text.trim()) 